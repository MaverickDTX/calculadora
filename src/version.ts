// Versão do app (fonte única de verdade — exibida na Sidebar).
// Convenção: pré-1.0, formato 0.x onde x é um contador inteiro que incrementa
// A CADA COMMIT (0.1 → 0.2 → … → 0.10 → 0.11). Bumpar esta linha em todo commit.
export const APP_VERSION = '0.14';
